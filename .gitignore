# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Git worktrees for Phase 1 parallel tracks
.worktrees/

# Env
.env
.env.*
!.env.example

# Intermediate LLM drafts from scripts/generate-scene-beats.ts — the final,
# hand-tuned output lives in src/data/, not here.
scripts/output/

# Kokoro voice style-embedding files used by scripts/generate-narration-audio.ts
# — large binaries, redownloadable from HuggingFace on demand, not source.
# (The generated public/narration/ WAV files + manifest.json ARE committed —
# unlike the above, those are real deployed static assets Vercel serves.)
scripts/tts-voices/

# Logs
logs
*.log
npm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
